#include "main.h"
/** 
 * _exit - exits
 * 
*
* void _exit(char **exit_args, size_t argc)
* {
*	int status = EXIT_SUCCESS;
*
*	
*	  if (exit_args[1])
	* {
		*status = atoi(exit_args[1]);
	* } 
	* free_vector(exit_args, argc);
	*exit (status);
	 */
	void _exit() /*check void*/
	{

	}
}